import { StateDistrictCount } from './state-district-count';

describe('StateDistrictCount', () => {
  it('should create an instance', () => {
    expect(new StateDistrictCount()).toBeTruthy();
  });
});
